[[bots]]
channel = "##horsing-around" # The channel this bot will support
format = "json" # Bot format (text, json, pickle)
path = "bots/##horsing-around" # Path to the chain/corpus to load
# language = "en" # Optioanl. Enable language specific built-in filters
# stopwords = [] # Optional. Pass ad hoc stopwords, that will be stripped from the input text
# log_query = false # If true, log text sent to the query endpoint, as well as the responses generated by the bot
